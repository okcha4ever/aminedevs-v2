# Amine Khiari Portfolio Website

## Installation
- Clone the repo and `cd` to the cloned directory named "aminedevs-v2"
- Run `npm install` or equivalent.
- Run `npm run dev` or equivalent to start the app.
